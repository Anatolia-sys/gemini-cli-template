# Gemini CLI テンプレート利用ガイド

> 社内向け Gemini CLI ベストプラクティステンプレートの使い方ガイドです。

---

## 目次

1. [はじめに](#はじめに)
2. [セットアップ手順](#セットアップ手順)
3. [フォルダ構成の全体像](#フォルダ構成の全体像)
4. [各ファイルの目的と編集方法](#各ファイルの目的と編集方法)
5. [カスタマイズガイド](#カスタマイズガイド)
6. [ベストプラクティスまとめ](#ベストプラクティスまとめ)
7. [FAQ・トラブルシューティング](#faqトラブルシューティング)

---

## はじめに

このテンプレートは、Gemini CLI を効果的に活用するための `.gemini` フォルダ構成一式です。
以下の調査・知見を反映しています：

- **Gemini CLI** 最新版（experimental機能含む）のベストプラクティス
- **Claude Code** のベストプラクティスで Gemini にも適用可能なもの
  - コンテキスト管理（300行以下のプロジェクトファイル）
  - 「Explore → Plan → Code」ワークフロー
  - スキル・サブエージェントのモジュール設計

---

## セットアップ手順

### 1. テンプレートをプロジェクトにコピー

```bash
# テンプレートフォルダをプロジェクトルートにコピー
cp -r template/.gemini /path/to/your-project/.gemini

# .geminiignore もプロジェクトルートにコピー
cp template/.geminiignore /path/to/your-project/.geminiignore
```

### 2. GEMINI.md を編集（最重要）

`.gemini/GEMINI.md` を開き、プロジェクトに合わせて以下を記述してください：

| セクション | 説明 | 優先度 |
|-----------|------|-------|
| プロジェクト概要 | 目的とゴール | ⭐⭐⭐ 必須 |
| 技術スタック | 使用する言語・フレームワーク | ⭐⭐⭐ 必須 |
| フォルダ構成 | プロジェクトのディレクトリマップ | ⭐⭐⭐ 必須 |
| 禁止事項 | AIが行ってはいけないこと | ⭐⭐⭐ 必須 |
| コーディング規約 | 主要なルールのサマリ | ⭐⭐ 推奨 |
| 外部参照 | 詳細ドキュメントへのリンク | ⭐ 任意 |

> **💡 ポイント**: GEMINI.md はコンテキストウィンドウを消費するため、**300行以下**に収めてください。詳細は別ファイルに分けて `@include` で参照します。

### 3. AGENT.md を確認・編集

エージェントのペルソナや作業ルールをプロジェクトに合わせて調整してください。
特に以下は必ず確認：

- 使用言語（デフォルト: 日本語）
- コードスタイル（テスト必須等）

### 4. settings.json を確認

モデル設定やチェックポイント機能を確認してください。

### 5. rules/ にコーディング規約を追加

`rules/_TEMPLATE.md` をコピーして、プロジェクトの言語に合わせた規約ファイルを作成してください。

```bash
# 例: TypeScript用の規約を作成
cp .gemini/rules/_TEMPLATE.md .gemini/rules/typescript.md
# エディタで編集
```

---

## フォルダ構成の全体像

```
project-root/
├── .geminiignore              # 🟢 AI除外設定（.gitignore形式）
└── .gemini/
    ├── GEMINI.md              # 🟢 プロジェクトコンテキスト（最重要）
    ├── AGENT.md               # 🟢 エージェント定義・振る舞い
    ├── settings.json          # 🟡 プロジェクト固有の設定
    ├── prompts/               # 🔵 汎用プロンプト集
    │   ├── code-review.md     #    コードレビュー依頼
    │   ├── refactoring.md     #    リファクタリング指示
    │   └── documentation.md   #    ドキュメント生成
    ├── tools/                 # 🔵 カスタムツール定義
    │   └── _TEMPLATE.md       #    テンプレート
    ├── agents/                # 🟡 サブエージェント（experimental）
    │   ├── _TEMPLATE.md       #    テンプレート
    │   └── code-reviewer/     #    サンプル: コードレビューエージェント
    │       └── agent.md
    ├── skills/                # 🟡 スキル定義
    │   ├── _TEMPLATE/         #    テンプレート
    │   │   └── SKILL.md
    │   └── tdd-workflow/      #    サンプル: TDDワークフロー
    │       └── SKILL.md
    ├── workflows/             # 🔵 開発フロー
    │   ├── development.md     #    開発フロー
    │   └── release.md         #    リリースフロー
    └── rules/                 # 🟢 コーディング規約
        ├── general.md          #    共通ルール
        └── _TEMPLATE.md        #    言語別テンプレート
```

**凡例**: 🟢 必須編集 ｜ 🟡 推奨編集 ｜ 🔵 必要に応じて編集

---

## 各ファイルの目的と編集方法

### .geminiignore（AI除外設定）

**目的**: Gemini CLI がコンテキストに含めないファイル・フォルダを指定  
**配置場所**: プロジェクトルート直下（`.gemini/` フォルダの外）  
**編集頻度**: プロジェクト初期に作成、依存関係追加時に更新  
**構文**: `.gitignore` と同じ形式（`#` コメント、`*` グロブ、`!` 否定パターン）  
**ヒント**:
- 機密情報（`.env`、鍵ファイル）は必ず除外する
- `node_modules/` 等の依存パッケージを除外するとパフォーマンスが大幅に向上
- ロックファイル（`package-lock.json` 等）は必要に応じてコメントアウトを解除

### GEMINI.md（プロジェクトコンテキスト）

**目的**: Gemini CLI がプロジェクトを理解するための最も重要なファイル。  
**編集頻度**: プロジェクト初期に作成し、大きな構造変更時に更新  
**ヒント**:
- Gemini CLI で `/init` コマンドを使うと自動生成される
- 変更後は `/memory refresh` でリロード

### AGENT.md（エージェント定義）

**目的**: エージェントの振る舞い・作業ルールを定義  
**編集頻度**: プロジェクト初期に作成  
**ヒント**: 
- 「Explore → Plan → Code」パターンを活用するとAIの出力品質が向上

### settings.json（設定）

**目的**: モデルやチェックポイント等のプロジェクト設定  
**編集頻度**: 初期設定後はほぼ変更なし  
**ヒント**: 
- チェックポイント（`checkpoint.enabled: true`）を有効にすると `/restore` で元に戻せる

### prompts/（プロンプト集）

**目的**: よく使うプロンプトのテンプレート化  
**編集頻度**: 必要に応じて追加  
**使い方**: プロンプト内のプレースホルダー `{...}` を実際の値に置き換えて使用

### tools/（カスタムツール）

**目的**: Gemini CLI で使用するカスタムツールの定義  
**編集頻度**: 必要に応じて追加  
**注意**: MCP連携は本テンプレートでは対象外

### agents/（サブエージェント）

**目的**: 特定タスクに特化したサブエージェントの定義  
**編集頻度**: 必要に応じて追加  
**⚠️注意**: experimental機能。YOLO mode で動作するため注意

### skills/（スキル）

**目的**: 再利用可能なタスク手順の定義  
**編集頻度**: よく繰り返す作業を発見したら追加  
**構造**: 各スキルは `スキル名/SKILL.md`（YAML フロントマター付き）

### workflows/（開発フロー）

**目的**: 開発・リリースプロセスの標準化  
**編集頻度**: プロセス変更時に更新

### rules/（コーディング規約）

**目的**: AIが従うべきコーディングルールの定義  
**編集頻度**: プロジェクト初期に作成  
**ヒント**: `general.md` の共通ルール + 言語別ファイルの組み合わせで管理

---

## カスタマイズガイド

### 言語・フレームワーク別の適用方法

テンプレートは汎用的に作られています。プロジェクトに合わせて以下を変更してください：

#### TypeScript/JavaScript プロジェクトの場合

1. `GEMINI.md` の技術スタック欄を更新
2. `rules/_TEMPLATE.md` → `rules/typescript.md` を作成
3. workflows の各コマンドを更新（`npm test`、`npm run build` 等）

#### Python プロジェクトの場合

1. `GEMINI.md` の技術スタック欄を更新
2. `rules/_TEMPLATE.md` → `rules/python.md` を作成（PEP 8準拠）
3. workflows の各コマンドを更新（`pytest`、`pip install` 等）

#### その他の言語

同様の手順で `rules/` に言語別ファイルを作成し、workflows のコマンドを更新してください。

### 新しいスキルの追加方法

```bash
# 1. テンプレートをコピー
cp -r .gemini/skills/_TEMPLATE .gemini/skills/新スキル名

# 2. SKILL.md を編集
# - YAML フロントマターの name / description を変更
# - 手順を記述
# - 必要に応じて scripts/ にスクリプトを追加
```

### 新しいサブエージェントの追加方法

```bash
# 1. ディレクトリを作成
mkdir .gemini/agents/新エージェント名

# 2. _TEMPLATE.md を参考に agent.md を作成
cp .gemini/agents/_TEMPLATE.md .gemini/agents/新エージェント名/agent.md

# 3. agent.md を編集
```

---

## ベストプラクティスまとめ

### GEMINI.md 作成のコツ

| ✅ やるべきこと | ❌ やってはいけないこと |
|---------------|---------------------|
| 300行以下に収める | すべての情報を詰め込む |
| `@include` で分割 | 巨大な1ファイルにする |
| 禁止事項を明記 | 暗黙の前提に頼る |
| 技術スタックを記載 | 自明な情報を省略する |

### エージェントとの対話のコツ

1. **具体的に指示する**: `@ファイル名` でファイルを参照、URL やスクリーンショットを活用
2. **検証手段を提供する**: テスト、期待出力、スクリーンショットを含める
3. **計画を先に立てる**: 「まず調査して、それから計画を見せてください」
4. **コンテキストを管理する**: 新しいトピックでは新しい会話を開始

### スキル・エージェント設計のコツ

1. **単一責任**: 1スキル/1エージェント = 1つの目的
2. **明確な入出力**: 何を受け取り何を返すかを定義
3. **再利用性**: プロジェクト横断で使えるよう汎用的に設計

---

## FAQ・トラブルシューティング

### Q: GEMINI.md を変更したのに反映されない
**A**: `/memory refresh` コマンドを実行してください。

### Q: settings.json が適用されない
**A**: 設定の優先順位を確認してください：
1. コマンドライン引数（最優先）
2. プロジェクト `.gemini/settings.json`
3. ユーザー `~/.gemini/settings.json`
4. デフォルト値

### Q: サブエージェントが期待通りに動かない
**A**: サブエージェントは experimental 機能です。以下を確認してください：
- `agent.md` のシステムプロンプトが明確か
- スコープが適切に定義されているか
- YOLO mode の動作に注意（個別承認なしで実行される可能性）

### Q: スキルが認識されない
**A**: 以下を確認してください：
- `SKILL.md` が存在するか
- YAML フロントマター（`name`、`description`）が正しく記述されているか
- ファイルが `.gemini/skills/スキル名/SKILL.md` の構造になっているか

### Q: チェックポイントから復元したい
**A**: `/restore` コマンドを実行してください。チェックポイントが有効（`settings.json` で `checkpoint.enabled: true`）の場合のみ機能します。

### Q: グローバルに適用したい設定がある
**A**: ユーザーレベルの設定は `~/.gemini/` ディレクトリに配置してください：
- `~/.gemini/GEMINI.md` - 全プロジェクト共通のコンテキスト
- `~/.gemini/settings.json` - 全プロジェクト共通の設定
- `~/.gemini/skills/` - 全プロジェクト共通のスキル
